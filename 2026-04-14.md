# SYTB Bericht
**Fach: SYTB**

**Thema: https://www.franzmatejka.at/htl/doc/SYTB_3/07_cmdsubst_ue.html**

**Name: Catalina Popa**

**Datum: 14.04.2026**

**Klasse: 3AHITS**


## Übung
1. Schreibe ein shellscript das als Argument einen Pfad auf ein Verzeichnis erhält. Das Script soll die Anzahl der Einträge in diesem Verzeichnis als Zahl ausgeben. Dies soll aus der ls Ausgabe mit wc ermittelt werden.

```
$ ./nbrentries.sh ~/test
Es sind 12 Einträge im dir ~/test
```

Hinweis – Vergleiche die Ausgaben von

```
ls
ls | cat
echo "$(ls)"
```

Dokumentiere den Unterschied, was ist die Logik dahinter.

meine Lösung:
```
┌──(kali㉿kali)-[~]
└─$ mkdir 260414    
┌──(kali㉿kali)-[~]
└─$ cd 260414
 
──(kali㉿kali)-[~/260414]
└─$ touch script.sh
┌──(kali㉿kali)-[~/260414]
└─$ chmod +x script.sh
 
┌──(kali㉿kali)-[~/260414]
└─$ touch test.txt 
┌──(kali㉿kali)-[~/260414]
└─$ touch text.txt 
┌──(kali㉿kali)-[~/260414]
└─$ touch test1.txt
┌──(kali㉿kali)-[~/260414]
└─$ touch test2.txt
┌──(kali㉿kali)-[~/260414]
└─$ touch test3.txt
 
┌──(kali㉿kali)-[~/260414]
└─$ ./script.sh ~/260324
Es sind 4 Einträge im dir /home/kali/260324
┌──(kali㉿kali)-[~/260414]
└─$ ./script.sh ~/260414
Es sind 6 Einträge im dir /home/kali/260414
┌──(kali㉿kali)-[~/260414]
└─$ ./script.sh ~/260407
Es sind 9 Einträge im dir /home/kali/260407
 
script:
#!/bin/bash
 
dir=$1
 
cd $dir
anzahl=$(ls | wc -l)
 
echo "Es sind $anzahl Einträge im dir $dir"
 
 
┌──(kali㉿kali)-[~/260414]
└─$ ./script.sh         
script.sh  test1.txt  test2.txt  test3.txt  test.txt  text.txt
script.sh
test1.txt
test2.txt
test3.txt
test.txt
text.txt
script.sh
test1.txt
test2.txt
test3.txt
test.txt
text.txt
```

Hier gibt es die Dateien doppelt aus ohne sie zu zählen

2. Schreibe ein Shell-Skript das die Anzahl der Tage bis zum HTL Ball ermittelt. Die Ausgabe soll in der folgenden Form sein.

```
Es sind noch 42 Tage bis zum HTL Ball (2027-01-16)
```

Hinweise:

Verwende das date Format für seconds since the Epoch (1970-01-01 00:00 UTC)

meine Lösung:
```
┌──(kali㉿kali)-[~/260414]
└─$ touch htlball.sh
 
┌──(kali㉿kali)-[~/260414]
└─$ chmod +x htlball.sh     
┌──(kali㉿kali)-[~/260414]
└─$ ./htlball.sh
Es sind noch 276 Tage bis zum 2027-01-16
 
script:
#!/bin/bash
 
ziel="2027-01-16"
 
tage=$(( ( $(date -d "$ziel" +%s) - $(date +%s) ) / 86400 ))
 
echo "Es sind noch $tage Tage bis zum $ziel"
```

3. Erstelle ein Skript das einen Satz aus 5 zufälligen Wörtern bildet, z.B.:

richtig ganzen Kilometer auf Mittel

Wähle die Wörter aus dieser Wortliste.

Hinweis: Das Komando sed -n 5p datei.txt gibt die 5te Zeile aus.

Erweiterung: Das Skript soll auch die Anzahl der in der Wortliste enthaltenen Wörter ermitteln. D.h. die Konstante 1000 soll im Skript nicht vorkommen.

meine Lösung:
```
┌──(kali㉿kali)-[~/260414]
└─$ touch random-satz.sh
┌──(kali㉿kali)-[~/260414]
└─$ touch words.txt     
┌──(kali㉿kali)-[~/260414]
└─$ echo "Freitag" > words.txt
┌──(kali㉿kali)-[~/260414]
└─$ echo "Familie" >> words.txt
┌──(kali㉿kali)-[~/260414]
└─$ echo "einige" >> words.txt 
┌──(kali㉿kali)-[~/260414]
└─$ echo "einmal" >> words.txt
┌──(kali㉿kali)-[~/260414]
└─$ echo "solche" >> words.txt
┌──(kali㉿kali)-[~/260414]
└─$ chmod +x random-satz.sh
┌──(kali㉿kali)-[~/260414]
└─$ ./random-satz.sh words.txt
solche Familie einige einige solche
 
script:
#!/bin/bash
 
file="$1"
 
anzahl=$(wc -l < "$file")
 
for i in 1 2 3 4 5
do
  zahl=$(( RANDOM % anzahl + 1 ))
  sed -n "${zahl}p" "$file"
done | tr '\n' ' '
echo
```

4. Create a script which will take a filename as its first argument and create a dated copy of the file. eg. if our file was named file1.txt and today is the 2021-10-29 it would create a copy such as 2021-10-29_file1.txt. (To achieve this you will probably want to play with command substitution and the command date)

```
┌──(kali㉿kali)-[~/260414]
└─$ touch copy.sh     
┌──(kali㉿kali)-[~/260414]
└─$ chmod +x copy.sh          
┌──(kali㉿kali)-[~/260414]
└─$ ./copy.sh words.txt
 
┌──(kali㉿kali)-[~/260414]
└─$ cat 2026-04-14_file       
Freitag
Familie
einige
einmal
solche

script:
 
#!/bin/bash
 
file="$1"
 
date=$(date -I)
 
cp "$file" "${date}_file"
```