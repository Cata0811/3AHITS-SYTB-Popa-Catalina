# SYTB Bericht
**Fach: SYTB**

**Thema: https://www.franzmatejka.at/htl/doc/SYTB_3/15_if_ue.html**

**Name: Catalina Popa**

**Datum: 05.05.2026**

**Klasse: 3AHITS**


## Übung

erste Aufgabe

Schreibe ein bash Skript das 2 Zahlen als Argumente aus der Kommandozeile übernimmt. Die größere der beiden Zahlen soll ausgegeben werden.

meine Lösung:

```
┌──(kali㉿kali)-[~]
└─$ mkdir 260505

┌──(kali㉿kali)-[~]
└─$ cd 260505  

┌──(kali㉿kali)-[~/260505]
└─$ touch larger.sh

┌──(kali㉿kali)-[~/260505]
└─$ chmod +x larger.sh
 
┌──(kali㉿kali)-[~/260505]
└─$ ./larger.sh 9 10
10
┌──(kali㉿kali)-[~/260505]
└─$ ./larger.sh 1 7 
7
┌──(kali㉿kali)-[~/260505]
└─$ ./larger.sh 6 1
6
 
larger.sh script:
#!/bin/bash
 
if [ $1 -gt $2 ]
then   
    echo "$1"
else 
    echo "$2"
fi
```

zweite Aufgabe

Schreibe ein Script das von einer als Argument übergebenen Zahl prüft ob sie gerade oder ungerade ist.

meine Lösung:

```
┌──(kali㉿kali)-[~/260505]
└─$ touch evenorodd.sh

┌──(kali㉿kali)-[~/260505]
└─$ chmod +x evenorodd.sh

┌──(kali㉿kali)-[~/260505]
└─$ ./evenorodd.sh 5
odd
┌──(kali㉿kali)-[~/260505]
└─$ ./evenorodd.sh 8
even
 
evenorodd.sh script:
#!/bin/bash
 
if [ $(($1 % 2)) -eq 0 ]
then
    echo "even"
else
    echo "odd"
fi
```

dritte Aufgabe

Schreibe ein Skript makedir.sh das mit einem Argument aufgerufen wird.

$ ./makedir.sh xyz

Ein Directory mit dem Namen xyz soll angelegt werden falls es nicht existiert. Im Directory lege eine Datei mit dem Namen xyz.txt und Inhalt xyz an.

Existiert das Directory bereits so soll gefragt werden ob das Directory gelöscht werden darf. Bei dieser Abfrage soll angegeben werden wie viele Files sich im Directory befinden.

Soll das Directory "xyz" (mit 5 Files) gelöscht werden? [j|n]: 

Auswahl j: Das Directory wird gelöscht und wieder wie oben angelegt.
Auswahl n: Das Skript wird beendet.

meine Lösung:

```
┌──(kali㉿kali)-[~/260505]
└─$ touch makedir.sh  

┌──(kali㉿kali)-[~/260505]
└─$ chmod +x makedir.sh
 
┌──(kali㉿kali)-[~/260505]
└─$ ls
evenorodd.sh  larger.sh  makedir.sh

┌──(kali㉿kali)-[~/260505]
└─$ ./makedir.sh

┌──(kali㉿kali)-[~/260505]
└─$ ls
evenorodd.sh  larger.sh  makedir.sh  xyz

┌──(kali㉿kali)-[~/260505]
└─$ cd xyz

┌──(kali㉿kali)-[~/260505/xyz]
└─$ ls
xyz.txt

┌──(kali㉿kali)-[~/260505/xyz]
└─$ cat xyz.txt
xyz
 
makedir.sh script:
#/bin/bash
 
if [ -d xyz ]
    then
    echo "das dir gibt es schon."
 
    cd xyz
 
    ls
 
    echo "löschen? (y/n)"
 
    read answer
 
    if [ "$answer" = "y" ]
    then
        rm -r xyz
        echo "verzeichnis gelöscht."
    else
        exit 0
    fi
 
fi
 
mkdir xyz
cd xyz
touch xyz.txt
 
echo "xyz" > xyz.txt
```

vierte Aufgabe 

hab ich nicht gemacht

fünfte Aufgabe

Schreibe ein Skript das aufgrund der aktuellen Zeit das aktuell Fach laut Stundenplan ausgibt. Pausen sind vereinfacht einem Fach zuzurechnen. Berücksichtige nur einen Wochentag, d.h. das Skript funktioniert nur an einem Donnerstag.

meine Lösung:

```
script: 

#/bin/bash
 
day=$(date $1| cut -d " " -f 5)
hour=$(date $4 | cut -d " " -f 5)
 
if [ date = Tue ] && [ hour >= 07:50:00 ] || [hour <= 09:35:00]
then
    echo "Dienstag $hour SYTB"
 
else
    echo "$day  $hour"
fi
```

Ich bin mir nicht sicher wegen dem script, weil ich es nicht testen konnte. Die Zeit auf meiner VM ist falsch und ich weiß nicht wo ich die richtig stellen kann.